# Project "About"

The GitHub **About** panel (the sidebar on the repo home page) is repo metadata,
not a file — set it under the ⚙️ next to "About". Suggested values below.

## Description (paste into About → Description)

> One ruleset, every agent. Compile token-saving rules into Claude Code, Codex
> and Copilot — plus task planning, a resource hub, and runtime output shrink.

(~150 chars; GitHub allows up to 350.)

## Website (About → Website)

Until a docs site exists, use the README anchor or the PyPI page once published:

- `https://github.com/keyurpatel446/Monolith#readme`
- or `https://pypi.org/project/monolith-ai/` (after the first publish)

## Topics (About → Topics)

Add these so the repo surfaces in GitHub topic search:

```
claude-code  codex  github-copilot  ai-agents  llm  agents
token-optimization  cost-optimization  prompt-engineering
developer-tools  cli  python  mcp  anthropic  openai
```

## One-liner / elevator pitch (reuse in posts and the website meta)

> Author your AI-coding rules once; Monolith compiles them into Claude Code,
> Codex, and Copilot — ~60–80% fewer output tokens, plus task management,
> a resource hub, and runtime `shrink`.

## Social preview image (optional but high-impact)

Settings → General → **Social preview** → upload a 1280×640 PNG with the 🧱
logo, the tagline, and the before/after token numbers. This is what shows when
the link is shared on X/LinkedIn/Slack.
