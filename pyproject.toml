[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "monolith-ai"
version = "0.1.7"
description = "Cross-agent token-efficiency layer for AI coding assistants (Claude Code, OpenAI Codex, GitHub Copilot)."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "Keyur Patel", email = "keyurpatel446@gmail.com" }]
keywords = ["claude-code", "codex", "copilot", "tokens", "llm", "cli", "ai-agents"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Environment :: Console",
]
# The core is intentionally dependency-free (stdlib only) for portable installs.
dependencies = []

[project.optional-dependencies]
# `pip install monolith-ai[bench]` enables exact token counts in `monolith
# bench`; without it the benchmark falls back to a heuristic counter.
bench = ["tiktoken>=0.5"]

[project.urls]
Homepage = "https://github.com/keyurpatel446/monolith"
Roadmap = "https://github.com/keyurpatel446/monolith/blob/main/ROADMAP.md"

[project.scripts]
monolith = "monolith.console:main"

[tool.hatch.build.targets.wheel]
packages = ["src/monolith"]
